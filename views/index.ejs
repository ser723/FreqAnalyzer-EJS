<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FreqAnalyzer Upload</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-gray-100 flex flex-col items-center justify-center min-h-screen p-4">
    <script src="https://cdn.tailwindcss.com"></script>
    <div class="w-full max-w-md bg-white p-8 rounded-xl shadow-2xl">
        <h1 class="text-3xl font-bold text-center text-indigo-700 mb-6">FreqAnalyzer 🎶</h1>
        <p class="text-center text-gray-600 mb-8">Upload an audio file (MP3, WAV, etc.) to analyze its dominant frequencies over time.</p>

        <form action="/analysis" method="POST" enctype="multipart/form-data" class="space-y-6">
            <div class="relative border-2 border-dashed border-gray-300 rounded-lg p-6 text-center hover:border-indigo-400 transition duration-300">
                <input type="file" name="audioFile" id="audioFile" accept="audio/*" required class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
                <label for="audioFile" class="block cursor-pointer">
                    <svg class="mx-auto h-12 w-12 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.885-7.143 5 5 0 0110.15 0A4 4 0 0117 16h-2"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 17l-4-4m0 0l-4 4m4-4v12"></path></svg>
                    <p class="mt-2 text-sm text-gray-600">Drag & drop or <span class="text-indigo-600 font-medium">Click to browse</span></p>
                    <p class="text-xs text-gray-500 mt-1">MP3, WAV, M4A, OGG (Max 10MB)</p>
                </label>
            </div>
            
            <button type="submit" class="w-full bg-indigo-600 text-white py-3 rounded-lg font-semibold hover:bg-indigo-700 transition duration-300 shadow-lg transform hover:scale-[1.01]">
                Start Analysis
            </button>
        </form>
    </div>
</body>
</html>